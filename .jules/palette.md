## 2026-07-07 - Ensure hover-revealed elements are keyboard accessible
**Learning:** Overlay containers that rely on `group-hover:opacity-100` to reveal interactive elements (like buttons) remain invisible to keyboard users when they tab to the element.
**Action:** Always pair `group-hover:opacity-100` with `focus-within:opacity-100` to ensure the elements become visible to keyboard users when focused.